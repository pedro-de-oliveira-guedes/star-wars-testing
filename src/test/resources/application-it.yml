spring:
    main:
        web-application-type: none
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
    datasource:
        url: ${MYSQL_HOST-TEST:jdbc:tc:mysql:8.0:///testdb?TC_IMAGE_TAG=8.0}